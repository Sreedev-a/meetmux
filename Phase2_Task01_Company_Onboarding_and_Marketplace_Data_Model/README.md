# Phase 2 Task 1 — Company Onboarding & Marketplace Data Model

This folder defines and validates the versioned matching feature space and backend contract.

```bash
../ml_env/bin/python validate_contract.py
../ml_env/bin/python -m unittest -v test_models.py
```
