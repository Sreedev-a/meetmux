# Submission — Phase 2 Task 1

Submit `FEATURE_SPACE.md`, `models.py`, `api/`, the sample request, validation output, tests, README, and `WRITTEN_ANSWER.md`. Run `../ml_env/bin/python validate_contract.py` and `../ml_env/bin/python -m unittest -v test_models.py`; capture the validation report and passing tests if screenshots are requested.
