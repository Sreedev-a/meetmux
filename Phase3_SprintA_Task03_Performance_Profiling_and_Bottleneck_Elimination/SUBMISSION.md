# Submission — Phase 3 Sprint A Task 3

Submit profiler/optimizer code, tests, decision document, `outputs/baseline_profile.txt`, before/after JSON, plot, README, and written answer. Run both README commands and capture the profile, exact quality match, before/after p95, cost proxy, and fallback. Limitation: results are hardware-local and must be rerun on production-class hosts; the catalog is a seeded fixture because no production feature snapshot is present.
