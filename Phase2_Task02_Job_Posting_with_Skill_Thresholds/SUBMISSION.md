# Submission — Phase 2 Task 2

Submit `match_vectors.py`, `data/sample_marketplace.json`, both files in `outputs/`, tests, README, and `WRITTEN_ANSWER.md`. Run `../ml_env/bin/python match_vectors.py` followed by `../ml_env/bin/python -m unittest -v test_match_vectors.py`; capture the generated-pair summary and passing tests if needed.
