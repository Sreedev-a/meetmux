# Phase 2 Task 2 — Job Posting with Skill Thresholds

Generates deterministic, ontology-normalized student/job vectors and validates competency thresholds. Run `python match_vectors.py` and `python -m unittest -v test_match_vectors.py` using the project environment.
