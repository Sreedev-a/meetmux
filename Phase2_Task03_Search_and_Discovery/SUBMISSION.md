# Submission — Phase 2 Task 3

Submit the ranker, sample marketplace data, `outputs/rankings.json`, tests, README, and written answer. Run the two README commands and capture both ranked sections plus passing tests if screenshots are required.
