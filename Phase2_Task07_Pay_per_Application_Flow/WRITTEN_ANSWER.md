# Pay-per-Application Flow

I tuned the ranking blend using offline conversion labels while enforcing Precision@3 of at least two relevant results out of three. The deterministic search records every relevance/conversion trade-off and selects the highest conversion configuration among only quality-feasible candidates. This protects paid-apply conversion without allowing a high-payment-propensity but irrelevant item to dominate unchecked.
